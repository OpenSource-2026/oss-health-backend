# OSS Health Checker — Backend Dependencies
# Python 3.12+

# --- Web framework ---
fastapi==0.115.0          # Async web framework with automatic OpenAPI docs
uvicorn[standard]==0.32.0 # ASGI server for FastAPI

# --- Validation & settings ---
pydantic==2.9.2           # Data validation and request/response schemas
pydantic-settings==2.6.0  # Environment variable loading via Pydantic models

# --- HTTP client ---
httpx==0.27.2             # Async HTTP client for GitHub API calls
requests==2.32.3          # Used by the vendored analysis pipeline (pipeline/)

# --- Analysis pipeline (data team's model inference) ---
# Slim inference set: the trained model is a scikit-learn LogisticRegression
# pipeline, so the heavier training-only deps (xgboost/lightgbm/catboost/shap)
# in pipeline/requirements.txt are intentionally NOT installed here.
# NOTE: scikit-learn is pinned to the version the model was trained/pickled
# with (per oss_health_model_metadata.json); an older minor raises
# InconsistentVersionWarning and may produce invalid scores.
scikit-learn==1.8.0       # Loads/runs the trained model (oss_health_best_model.joblib)
pandas==3.0.3             # Feature engineering in pipeline/features
numpy==2.4.6              # Numeric ops for feature/percentile scoring
scipy==1.17.1             # scikit-learn runtime dependency
joblib==1.4.2             # Deserializes the model artifact

# --- Observability ---
prometheus-fastapi-instrumentator==7.0.0  # /metrics endpoint for Prometheus/Grafana

# --- AI ---
google-generativeai==0.8.3  # Gemini API client for the optional AI report

# --- Utilities ---
python-dotenv==1.0.1      # Load .env files in local development

# --- Development & test ---
pytest==8.3.3             # Test framework
pytest-asyncio==0.24.0    # Async test support for FastAPI endpoints
ruff==0.7.1               # Linter / formatter
mypy==1.13.0              # Static type checker
