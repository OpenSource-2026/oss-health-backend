[pytest]
# Only collect the backend's own test suite. The vendored analysis engine
# under pipeline/ ships an executable smoke_test.py that is a CLI script, not
# a pytest module, so it must not be collected.
testpaths = tests
addopts = -q
asyncio_default_fixture_loop_scope = function
